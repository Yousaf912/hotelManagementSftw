import { createContext } from "react";

export const Store = createContext<any>({})
export const StoreTwo = createContext<any>({})
export const ComonStore = createContext<any>({})
export const ProfileStore = createContext<any>({})

